syntax = "proto3";

package interaction.event;

option go_package = "github.com/Yux77Yux/platform_backend/generated/interaction/event;";

enum Exchange {
  EXCHANGE_COMPUTE_SIMILAR_CREATION = 0;
  EXCHANGE_COMPUTE_USER = 1;
}

enum RoutingKey {
  KEY_COMPUTE_SIMILAR_CREATION = 0;
  KEY_COMPUTE_USER = 1;
}